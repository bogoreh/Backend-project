<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Active Elections - E-Voting System</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        .card-hover {
            transition: transform 0.3s ease;
        }
        .card-hover:hover {
            transform: translateY(-5px);
        }
        .vote-btn {
            background: linear-gradient(45deg, #667eea, #764ba2);
            border: none;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="fas fa-vote-yea"></i> E-Voting System
            </a>
            <div class="navbar-nav ms-auto">
                <a class="nav-link" href="/elections">Elections</a>
                <form action="/logout" method="POST" class="d-inline">
                    <button type="submit" class="btn btn-outline-light btn-sm">Logout</button>
                </form>
            </div>
        </div>
    </nav>

    <div class="container mt-5">
        <h2 class="mb-4">Active Elections</h2>
        
        <% if (elections.length === 0) { %>
            <div class="alert alert-info">
                <h4><i class="fas fa-info-circle"></i> No active elections at the moment</h4>
                <p class="mb-0">Check back later for upcoming elections.</p>
            </div>
        <% } else { %>
            <div class="row">
                <% elections.forEach(election => { %>
                    <div class="col-md-6 mb-4">
                        <div class="card card-hover shadow">
                            <div class="card-body">
                                <h5 class="card-title"><%= election.title %></h5>
                                <p class="card-text text-muted"><%= election.description %></p>
                                <div class="mb-3">
                                    <small class="text-muted">
                                        <i class="fas fa-calendar"></i>
                                        Ends: <%= election.endDate.toLocaleDateString() %>
                                    </small>
                                </div>
                                <a href="/elections/<%= election._id %>" class="btn vote-btn text-white">
                                    View Election <i class="fas fa-arrow-right"></i>
                                </a>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>
        <% } %>
    </div>

    <footer class="bg-dark text-white text-center py-4 mt-5">
        <div class="container">
            <p>&copy; 2024 E-Voting System. All rights reserved.</p>
        </div>
    </footer>
</body>
</html>